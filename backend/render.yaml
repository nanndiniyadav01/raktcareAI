services:
  - type: web
    name: raktcare-api
    env: python
    buildCommand: pip install -r requirements.txt && python ml_model_extractor.py
    startCommand: uvicorn ml_backend:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
